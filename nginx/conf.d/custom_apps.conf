# 所有站点统一使用 Port 80，通过域名区分
# 解决了端口占用问题 (8300-8900 依然由本地服务使用)

# Domain: docker.yaanair.org:8080 -> /Air_Pollution/public/
server {
    listen 80;
    server_name docker.yaanair.org;
    charset utf-8;
    root /var/www/html/Air_Pollution/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: emergency.docker.yaanair.org:8080 -> /Emergency_Command_Platform/public/
server {
    listen 80;
    server_name emergency.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/Emergency_Command_Platform/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: lot.docker.yaanair.org:8080 -> /LOT/public/
server {
    listen 80;
    server_name lot.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/LOT/public;
    index index.php index.html;

    location = /admin/index.html {
        return 302 /index.html;
    }

    location / {
        index index.html index.htm index.php;
        try_files $uri $uri/ /index.php?_url=$uri&$args;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: remotegas.docker.yaanair.org:8080 -> /WWW/RemoteGas/public/
server {
    listen 80;
    server_name remotegas.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/RemoteGas/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: voc.docker.yaanair.org:8080 -> /WWW/Voc/public/
server {
    listen 80;
    server_name voc.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/Voc/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: env.docker.yaanair.org:8080 -> /WWW/Env_Inspection/public/
server {
    listen 80;
    server_name env.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/Env_Inspection/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: polluter.docker.yaanair.org:8080 -> /WWW/polluter/public/
server {
    listen 80;
    server_name polluter.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/polluter/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}

# Domain: data.docker.yaanair.org:8080 -> /WWW/EnvironmentData/public/
server {
    listen 80;
    server_name data.docker.yaanair.org;
    charset utf-8;
    root /var/www/html/EnvironmentData/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_pass php-dm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_param HTTP_HOST $host;
        fastcgi_param HTTPS $https if_not_empty;
    }
}
